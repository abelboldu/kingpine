---
- hosts: all
  roles:
    - role: leanbit.netdata
      netdata_version: 'v1.5.0'

  tasks:
    - name: Display all variables/facts known for a host
      debug:
        var: hostvars
        verbosity: 4
